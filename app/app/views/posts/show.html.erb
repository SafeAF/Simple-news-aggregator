<p id="notice"><%= notice %></p>

<div class="container mb-5 mt-5">
    <div class="card">
        <div class="row">
            <div class="col-md-12 ">
            	
            		
            	 
            	

<h5><%= link_to @post.headline.capitalize, @post.url %></h5></h5>


            	        
<p> <%= render 'posts/like_link', post: @post %> <%= render 'posts/likes', post: @post %> | <%= link_to "#{@post.user.username}", user_path(@post.user.id) %> <%= time_ago_in_words @post.created_at %> ago

               <% if current_user && current_user.moderator %>
                                                <%= link_to 'Destroy', post_path(@post.id), method: :delete, data: { confirm: 'Are you sure?' } %>
                                          <% end %>
                
                </p> 
<p><%= @post.body %></p>

</div>
</div>
</div>
</div>



<div id="post-comments">
<%= render partial: "posts/comments", locals: { comments: @comments } %>
</div>


 
