<p id="notice"><%= notice %></p>

<div class="container mb-5 mt-5">
    <div class="card">
        <div class="row">
            <div class="col-md-12 ">
            	
            		
            	 
            	

<h5><%= link_to @post.headline.capitalize, @post.url %></h5></h5>


            	        
<p> <%= render 'posts/like_link', post: @post %> <%= render 'posts/likes', post: @post %> | Submitted by <%= link_to "#{@post.user.username}", user_path(@post.user.id) %> <%= time_ago_in_words @post.created_at %> ago</p>
<p><%= @post.body %></p>

</div>
</div>
</div>
</div>



<div id="post-comments">
<%= render partial: "posts/comments", locals: { comments: @comments } %>
</div>


 
