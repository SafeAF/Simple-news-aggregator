<%= vote_text = current_user&.voted_for?(comment) ? "Unlike" : "Like" %>

<%= link_to vote_text, like_post_comment_path(post_id: @post.id, comment_id: comment.id), method: :put, remote: :true, class: 'like-post' %>